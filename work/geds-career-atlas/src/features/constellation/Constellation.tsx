import { buildPackLayout } from "./layout"
export function Constellation({nodes,focus,onFocus}:{nodes:{id:string;name:string;value?:number}[];focus?:string;onFocus?:(id:string)=>void}){const positioned=buildPackLayout(nodes);return <section aria-label="Government constellation"><svg viewBox="0 0 800 520" aria-hidden="true">{positioned.map(node=><g key={node.id}><circle cx={node.x} cy={node.y} r={Math.max(8,node.r)} fill={node.id===focus?"#42e8ee":"#4190ff"}/><text x={node.x} y={node.y} textAnchor="middle" fill="#f4f8ff">{node.name}</text></g>)}</svg><div role="listbox" aria-label="Government map">{nodes.map(node=><button key={node.id} role="option" aria-selected={node.id===focus} onClick={()=>onFocus?.(node.id)}>{node.name}</button>)}</div></section>}
