import { deterministicLayout } from "./layout"
export function Constellation({nodes,focus}:{nodes:{id:string;name:string}[];focus?:string}){const positioned=deterministicLayout(nodes);return <section aria-label="Government constellation"><svg viewBox="0 0 800 520" aria-hidden="true">{positioned.map(node=><g key={node.id}><circle cx={node.x} cy={node.y} r={node.id===focus?16:10} fill={node.id===focus?"#42e8ee":"#4190ff"}/><text x={node.x+18} y={node.y+4} fill="#f4f8ff">{node.name}</text></g>)}</svg><div role="listbox" aria-label="Constellation organizations">{nodes.map(node=><button key={node.id} role="option" aria-selected={node.id===focus}>{node.name}</button>)}</div></section>}
